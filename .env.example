# Add the replacement key in Vercel or .env.local.
# Never commit a real key.
NEIS_API_KEY=

# SchoolInfo OpenAPI key. The official endpoint below is optional because it is
# already the server default; overrides must remain on an HTTPS schoolinfo.go.kr host.
SCHOOLINFO_API_KEY=
# SCHOOLINFO_API_URL=https://www.schoolinfo.go.kr/openApi.do
